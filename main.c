#include <stdio.h>
#include <string.h>

int main(int argc, char **argv)
{
   ;
    gets(last); /* buffer overflow? what's that? */


    printf("Hello, %s %s!\n", first, last);
fgvfj

    return 0;
}
